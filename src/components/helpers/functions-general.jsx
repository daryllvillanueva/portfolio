// access image
export const baseImgUrl = "../../img";
export const baseSoundUrl = "../../sounds";

// for page title
export const PageTitle = (newTitle) => {
    return (document.title = newTitle);
};
